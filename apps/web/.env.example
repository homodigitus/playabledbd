# Browser-visible base URL for the Fastify API. Baked in at build time (Next.js inlines
# NEXT_PUBLIC_* vars), so this must be set before `next build` runs in any environment.
NEXT_PUBLIC_API_URL=http://localhost:4000
